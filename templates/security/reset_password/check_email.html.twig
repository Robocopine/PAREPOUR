{% extends 'app/base.html.twig' %}

{% block title %}{{ controller_name }}{% endblock %}

{% block body %}
    <section class="container">
        <div class="row justify-content-center">
            <div class="col-10">
                <p>
                    {% trans %}Si un compte correspondant à votre adresse e-mail existe, un e-mail contenant un lien pour réinitialiser votre mot de passe vient d'être envoyé.{% endtrans %}
                    {% trans %}Ce lien expire dans{% endtrans %} {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>{% trans %}Si vous n'avez pas reçu d'email, vérifiez vos spams sinon {% endtrans %} <a href="{{ path('forgot_password_request') }}">{% trans %}Réessayer{% endtrans %}</a>.</p>
            </div>
        </div>
    </section>
{% endblock %}
