<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="Julie Böttcher, Webmaster volontaire" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('src/img/ballon.png') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
    </head>
    <body id="page-top">
        {% block background %}{% endblock %}
        <!-- Navigation-->
        {{ include("app/partials/_navbar.html.twig") }}
        <!-- Header-->
        {% block header %}{{ include("app/partials/_header.html.twig") }}{% endblock %}
        {% block body %}{% endblock %}
        <!-- Contact section-->
        {{ vars.display() }}
        <!-- Footer-->
        {{ include("app/partials/_footer.html.twig") }}

        <!-- Bootstrap core JS-->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </body>
</html>
