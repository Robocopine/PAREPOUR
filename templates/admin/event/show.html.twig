<!DOCTYPE html>

<title>event</title>

{% block body %}
    <h1>event</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event.id }}</td>
            </tr>
            <tr>
                <th>TitleFr</th>
                <td>{{ event.titleFr }}</td>
            </tr>
            <tr>
                <th>TitleNl</th>
                <td>{{ event.titleNl }}</td>
            </tr>
            <tr>
                <th>TitleEn</th>
                <td>{{ event.titleEn }}</td>
            </tr>
            <tr>
                <th>ResumeFr</th>
                <td>{{ event.resumeFr }}</td>
            </tr>
            <tr>
                <th>ResumeNl</th>
                <td>{{ event.resumeNl }}</td>
            </tr>
            <tr>
                <th>ResumeEn</th>
                <td>{{ event.resumeEn }}</td>
            </tr>
            <tr>
                <th>DescriptionFr</th>
                <td>{{ event.descriptionFr }}</td>
            </tr>
            <tr>
                <th>DescriptionNl</th>
                <td>{{ event.descriptionNl }}</td>
            </tr>
            <tr>
                <th>DescriptionEn</th>
                <td>{{ event.descriptionEn }}</td>
            </tr>
            <tr>
                <th>DateTime</th>
                <td>{{ event.dateTime ? event.dateTime|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ event.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_event_index') }}">back to list</a>

    <a href="{{ path('app_admin_event_edit', {'id': event.id}) }}">edit</a>

    {{ include('admin/event/_delete_form.html.twig') }}
{% endblock %}
